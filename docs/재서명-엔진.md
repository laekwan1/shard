# iOS 자체 서명 엔진 — 설계 (진행 중)

Shard가 **SideStore 없이 자기 자신(과 Veil)을 재서명**한다. 처음엔 Sideloadly로 1회
설치하고, 이후엔 이 엔진이 갱신한다. **repo는 private 유지** — 그래서 **AGPL(SideStore)
코드는 참조·차용 금지**, **공개 프로토콜 + 관대 라이선스 부품**으로만 구현한다.

목표는 SideStore 전체가 아니라 **"서명 + 설치 갱신" 코어 4조각**만 가볍게 넣는 것.

## 라이선스 (결정 기록 — 개인 사용이라 사실상 무제한)
- Shard 워크스페이스는 이미 **GPL-3.0**. SideStore는 **AGPLv3** — GPLv3와 **호환**.
- 무엇보다 **개인 사용만**(배포 안 함) → GPL/AGPL의 **소스 제공 의무가 발동 안 함**. 즉
  **SideStore의 AGPL Rust 코어를 자유롭게 포팅**해도 된다. (혹시 나중에 배포하게 되면 그때
  소스 제공을 챙기면 되고, 그래도 GPL/AGPL 호환이라 합법.)
- 그래서 **③④는 재구현이 아니라 SideStore의 검증된 Rust를 포팅**한다 — 수 주 절약 + 안정성.
- **LocalDevVPN은 못 없앤다**(설치 터널 엔타이틀먼트는 무료 사이드로드 앱이 못 가짐).
  → 최종 앱 구성 = **Shard + LocalDevVPN**(SideStore 제거, 추가 앱 1개).
- 애플 강제 한계는 그대로: **7일 만료**, 만료된 앱은 자가 실행 불가(부트스트랩).

## 4개 부품과 재료 (③④는 SideStore 포팅으로 확정)

| # | 부품 | 무엇 | 재료 |
|---|---|---|---|
| ① | **서명** | .ipa를 인증서+프로파일로 서명 | **`apple-codesign`(Apache/MPL, 순수 Rust)** — 1단계 PoC로 검증됨. 그대로 |
| ② | **Developer API** | 인증서·App ID·프로파일 발급/갱신 | 우리가 직접 HTTP(엔드포인트 공개). SideStore/AltStore 흐름 참고 가능 |
| ③ | **anisette** | Apple ID 인증 토큰 | **SideStore/AltStore anisette(omnisette 등) 포팅**. 애플이 자주 깨는 부분 = 유지보수 핵심 |
| ④ | **설치** | lockdownd `installation_proxy`로 설치 | **minimuxer 포팅**(사용자 확정). LocalDevVPN 터널 + 페어링 파일 경유 |

④는 **LocalDevVPN 터널 + 페어링 파일**을 경유해 폰의 lockdownd에 닿는다.

## 재서명 = 재설치
재서명은 새 서명이 박힌 .ipa를 만드는 것이고, 그걸 **④ 설치로 다시 설치**해야 유효기간이
연장된다(설치가 기존 앱을 덮어씀). 즉 **재서명 = 재설치**. 실행 중인 인스턴스는 설치 중/후
종료될 수 있다.

## 재서명 타이밍 (사용자 확정)
- **매 실행마다는 안 함**(재설치가 매번 → 느림·낭비).
- **포그라운드/백그라운드 둘 다**에서 인증서 만료를 확인하고, **만료 2일 이내면 자동 재서명**.
  - 포그라운드 진입(콜드 실행 + 돌아올 때마다) → "안 끄고 계속 사용"도 커버.
  - 백그라운드는 **BGTaskScheduler**로 시도 — 단 iOS 백그라운드 실행은 보장이 없어
    **best-effort**(SideStore 자체 백그라운드 갱신도 자주 실패). 그래서 포그라운드 확인이 주,
    백그라운드가 보조.
- **UI — 주소창 오른쪽 모래시계**: 남은 기간 표시, **탭 = 수동 재서명**. 모래시계는 주소창에서
  **한 번 더 오른쪽 스와이프**해 **우회 on/off 토글이 뜰 때 함께** 노출한다(별도 상시 노출 아님).
- **설정**: Apple ID(+앱 암호) 저장. 없으면 모래시계 탭 시 "계정 저장·재서명 안 됨" 알림.
- 자동/수동 **둘 다** 둔다(택일 아님).

## 단계 (PoC → 확장)
1. ✅ **① 서명 PoC (완료, d877c88)**: `crates/resign`에 `apple-codesign`가 컴파일·실행되고
   서명 API에 닿음을 확인. 다음: `default-features=false`로 서명 코어만 남겨 iOS용 경량화 +
   더미 신원으로 .ipa 실제 서명까지.
2. **② Developer API**: 무료 계정 7일 인증서·App ID·프로파일 발급 흐름. SideStore/AltStore 참고.
3. **④ 설치 — minimuxer 포팅**: SideStore minimuxer를 가져와 LocalDevVPN 터널 경유 lockdownd
   설치. **실기기 필요** — 실현성의 관건.
4. **③ anisette 포팅**: SideStore/AltStore anisette로 Apple ID 인증. **또 다른 관건.**
5. **엔진 통합 + UI**(모래시계·설정·자동 트리거·재설치), iOS/Rust 연결.

**③④가 실현성의 핵심** — ①은 검증됐으니, 포팅한 ③④를 실기기로 일찍 실증한다.

## 규모
포팅 방식이라 재구현보다 가벼움(그래도 통합·iOS 연결로 수 주). 엔진 결합보다 우선(사용자 지시).
검증은 **실기기(iOS)** 가 있어야 ③④가 된다 — PC 헤드리스로는 ①②까지. 다음 세션에서 폰 붙여
③④ 이어가기.
