<?xml version="1.0" encoding="utf-8"?>
<!--
    Back: a chevron, nothing else.

    Fourth and simplest drawing of this glyph — the filled arrow, the stroke
    arrow and the rounded triangle each carried more shape than the job needs.
    Grey like the switch's unselected labels; stroke, so it cannot read
    brighter than they do the way the filled triangle did.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="@color/muted">
    <path
        android:strokeColor="#FFFFFF"
        android:strokeWidth="2.4"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M14.4,5.6 L8,12 L14.4,18.4" />
</vector>
